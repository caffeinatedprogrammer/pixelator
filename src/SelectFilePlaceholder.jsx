import React from "react";

export default function SelectFilePlaceholder({className}) {
    return (
        <div className={`center ${className}`}>
            <span>Select a file</span>
        </div>
    );
}